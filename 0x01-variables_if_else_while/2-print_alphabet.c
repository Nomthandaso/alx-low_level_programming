#include <stdio.h>
/**
 * main
 *
 * Description: 'putchar the alphabet in lowercase'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;
	for (ch = 'a' ; ch <= 'z' ; ch++)
		putchar ("ch\n");
	return(0);
}
