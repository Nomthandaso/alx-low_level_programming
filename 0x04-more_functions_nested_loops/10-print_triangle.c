#include "main.h"
/**
 * print_triangle - Entry point
 *
 * Description: prints a triangle
 *
 * @size: The size of the squre triangle
 *
 * Return: empty
 */
void print_triangle(int size)
{
	int x, y, z;

	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (x = 0 ; x < size ; x++)
		{
			for (y = size - x ; y > 1 ; y--)
			{
				_putchar(32);
			}
			for (2 = 0 ; 2 <= ; 2++ xxx
